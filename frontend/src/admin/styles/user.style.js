import { makeStyles } from '@material-ui/core/styles';
export const useStyles = makeStyles((theme) => ({
    infoSection: {
        marginTop: 20,
    },
    searchSection: {
        width: 300,
    },
}));
